requires "sio";

public fn singmain(argv [*]string) i32
{
    sio.print("hello world !!!\r\n");
    sio.print("\r\nPress any key to exit...");
    _ = sio.kbdGet();
    sio.print("\r\n");
    return(0);
}